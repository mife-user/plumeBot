# reasonix session

## User

当前我要开发一款bot，基于onebot协议，当前准备先对接qq的napcat，用go语言开发，请先搜索相关同样的项目，然后我们探讨后续计划

## Assistant

好的，让我先了解一下你的工作区现状，同时搜索相关的开源项目和资料。

让我进一步深入搜索 Go 语言生态中的关键 OneBot 项目。

让我再看看几个核心项目的架构和 OneBot 协议标准。

## User

The previous assistant response was interrupted during streaming. Continue the same task now. Partial text remains visible to the user but was excluded from model context; avoid needlessly repeating it, and do not assume it was complete.

The previous assistant response was interrupted during streaming before visible answer text was completed. Continue the same task now and provide the next useful response.

## Assistant

好的，我已经完成了对 OneBot + NapCat + Go 生态的全面调研。下面是总结和建议。

---

## 🔍 生态调研总结

### 一、协议层：OneBot

OneBot 是聊天机器人应用接口标准，目前主流版本是 **OneBot v11**，新标准 **v12** 也在推进中。通信方式：

| 方式 | 说明 |
|------|------|
| **正向 WebSocket** | bot 主动连接 OneBot 实现端 |
| **反向 WebSocket** | OneBot 实现端反向连接 bot |
| HTTP API | 通过 HTTP 调用 API |
| HTTP POST | 事件通过 HTTP POST 推送到 bot |

### 二、QQ 端：NapCat

**[NapCatQQ](https://github.com/NapNeko/NapCatQQ)**（10k ⭐）是目前最活跃的 QQ OneBot 实现，基于 NTQQ 原生协议，TypeScript 编写，完美支持 OneBot v11。

### 三、Go 语言生态（核心发现）

| 项目 | Stars | 定位 | 状态 |
|------|-------|------|------|
| **[ZeroBot](https://github.com/wdvxdr1123/ZeroBot)** | 398 | OneBot v11 框架核心 | 活跃 |
| **[ZeroBot-Plugin](https://github.com/FloatTech/ZeroBot-Plugin)** | 2.6k | 基于 ZeroBot 的插件合集（100+ 插件） | 活跃 |
| **[xbot](https://github.com/xiaoyi510/xbot)** | 12 | 现代化 Go OneBot 框架 | 新项目 |
| **[CuteReimu/onebot](https://github.com/CuteReimu/onebot)** | 4 | OneBot Go SDK | 活跃 |
| **[Gensokyo](https://github.com/Hoshinonyaruko/Gensokyo)** | 42 | QQ 官方 API → OneBot 标准桥 | 活跃 |
| **[go-cqhttp](https://github.com/Mrs4s/go-cqhttp)** | 已归档 | 曾经的 Go OneBot 标杆 | ❌ 停止维护 |

### 四、关键判断

1. **ZeroBot 是 Go 生态的事实标准**——2.6k 的插件项目生态证明了它的成熟度
2. NapCat 已明确被 ZeroBot-Plugin 推荐为上游
3. 现有 Go OneBot SDK 要么体量太大（ZeroBot-Plugin 偏重），要么太轻量、不成熟
4. **缺少一个"轻量、现代化、开箱即用"的 Go OneBot SDK/bot 框架**，专门面向 NapCat 场景

---

## 🤔 后续方向探讨

基于以上调研，我想和你讨论几个关键决策：

好问题！让我先把 ZeroBot 在整体架构中的位置讲清楚。

---

## 🏗️ OneBot 生态架构解释

```
┌──────────┐     QQ 私有协议      ┌──────────────┐    OneBot v11 (WebSocket/HTTP)    ┌─────────────────┐
│  QQ 服务器 │ ◄──────────────────► │   NapCatQQ    │ ◄──────────────────────────────► │  你的 Go Bot 程序  │
└──────────┘                      └──────────────┘                                   └─────────────────┘
                                   "实现端" (Implement)                               "应用端" (Consumer)
                                   负责：登录QQ、收发消息、                              负责：接收事件、处理逻辑、
                                   把QQ消息转为OneBot标准格式                              调用API发消息/操作
```

- **NapCatQQ** 是 OneBot 协议的「实现端」——它登录你的 QQ 号，和腾讯服务器通信，把所有事件（消息、通知、请求）转成 OneBot 标准 JSON，通过 WebSocket 发出来；同时也接收 OneBot 格式的 API 调用来执行操作（发消息、踢人、设置头像等）。

- **ZeroBot** 是 OneBot 协议的「应用端框架」——它就是一个 Go 库，帮你处理 WebSocket 连接、解析 OneBot JSON 消息、提供事件注册/分发机制、封装了常用 API。**它不直接连 QQ，它连的是 NapCat**。

- 你的业务代码就是在 ZeroBot（或其他框架/SDK）之上写处理逻辑。

---

## 🤔 该不该用 ZeroBot？

根据你的需求（AI 对话 + 命令分发 + 完整适配 NapCat 的各种消息/API），我的分析：

| 考量 | ZeroBot | 自研轻量 SDK |
|------|---------|-------------|
| **学习成本** | 需要理解它的插件体系 | 从零写，但完全可控 |
| **成熟度** | 稳定，有大量现成插件参考 | 需要自己踩坑 |
| **灵活性** | 受限于框架设计 | 完全自由 |
| **NapCat 适配** | 基于 OneBot v11 通用协议，兼容性好 | 你可以针对性优化 |
| **代码量** | `init()` 注册 + handler | 需要自己写 WebSocket、事件解析、API 封装 |

**我的建议：先用 ZeroBot 快速跑起来，再决定。**

理由很实际——先让 bot 能收到消息、能回复，建立正反馈。如果后面觉得 ZeroBot 的限制太多（比如你想做一些框架没有封装的原生 NapCat 操作），再渐进去掉它也不迟。ZeroBot 本质上就是一个 `go get` 的依赖，替换成本不高。

你觉得这个思路可以吗？还是你倾向于一开始就完全自主可控？

## User

可以，用zerobot帮忙连接napcat，我专注实现bot，但是为什么是zerobot来实现插件而不是bot层

## Assistant

这个问题问到点子上了！让我把分层画清楚，这里确实容易误解：

## 📦 分清楚三个东西

```
┌─────────────────────────────────────────────────────┐
│  ZeroBot-Plugin（别人写的 100+ 业务功能）               │
│  抽卡 / 签到 / AI聊天 / 点歌 / 涩图 / 牛牛大作战 ...     │   ← 这是"别人家的bot"
├─────────────────────────────────────────────────────┤
│  你的 bot 业务代码                                     │
│  /help /ping /天气 / 你的自定义逻辑 ...                 │   ← 这是你要写的
├─────────────────────────────────────────────────────┤
│  ZeroBot 框架（底层，只管连接和事件路由）                 │
│  WebSocket连接 / JSON解析 / 事件分发 / 规则匹配引擎      │   ← 这是个纯库
├─────────────────────────────────────────────────────┤
│  NapCatQQ                                             │   ← QQ 登录和消息收发
└─────────────────────────────────────────────────────┘
```

---

## 🎯 ZeroBot 具体做了什么？

它不是"插件系统"，它是一个**事件路由框架**。类比 web 开发：

| 概念 | Web 框架 (如 Gin) | ZeroBot |
|------|-------------------|---------|
| 连接管理 | `http.ListenAndServe` | WebSocket 连接 NapCat |
| 请求解析 | JSON body → struct | OneBot JSON → Go struct |
| 路由匹配 | `router.GET("/ping", handler)` | `engine.OnCommand("ping", handler)` |
| 中间件 | `r.Use(logger)` | `engine.Use(recovery)` |
| 上下文 | `ctx.JSON(200, data)` | `ctx.Reply("pong")` |

ZeroBot **不绑定任何业务**。你写的 `main.go` 长这样：

```go
package main

import "github.com/wdvxdr1123/ZeroBot"

func main() {
    // 这就启用了 ZeroBot 的底层能力：连接 + 事件分发
    ZeroBot.Run(ZeroBot.Config{
        NickName:      []string{"小助手"},
        CommandPrefix: "/",
        SuperUsers:    []int64{你的QQ号},
    })
}
```

你通过 `init()` 注册你自己的业务：

```go
// 你自己的业务文件 myplugin/weather.go
package myplugin

import "github.com/wdvxdr1123/ZeroBot"

func init() {
    engine := ZeroBot.NewEngine()

    // 这是你的业务逻辑，不是 ZeroBot 的插件
    engine.OnCommand("天气").Handle(func(ctx *ZeroBot.Ctx) {
        ctx.Reply("今天晴天，25°C")
    })
}
```

**ZeroBot-Plugin 和你写的东西是同一层级**——都只是用 ZeroBot 框架注册 handler。你可以完全不 import ZeroBot-Plugin，只写自己的业务。

---

## 🔌 所以"插件"是你自己写的

简单总结一句话：

> **ZeroBot = WebSocket 客户端 + 规则路由器**，它只管「连接 NapCat」和「把消息路由到正确的 handler」。业务逻辑（插件）是你 `init()` 里注册的那些 handler。

这个分层很合理——就像你不会自己从零写 HTTP 库再写 web 应用，而是用 Gin/Fiber 直接写业务一样。

要不要我现在就帮你搭一个最小可跑的 bot 骨架？

## User

继续讨论相关设计，关于连接napcat和实现napcat逻辑就用zerobot了，现在讨论其他层面框架选择1.bot的agent采用eino 2.相关群聊对话，私聊对话如何存储，，群聊对话中单个人和整个群聊对话如何设计 3.插件设计如何实现，有什么框架可用 4.如何让ai像真人一样处理各种事件等

## Assistant

好问题！这几个都是决定 bot "智商"和"人格"的核心设计。让我逐一深入分析。

先看看当前工作区状态，同时查一下 eino 和相关方案。

MumuBot 这个项目非常关键——它和你想要做的几乎完全一致！让我看看它的内部结构来给你更好的设计参考。

## User

继续

## Assistant

好的，基于之前的调研，特别是 MumuBot（它和你目标几乎一致，Go + eino + NapCat）的架构，我们来逐一深入讨论这四个设计问题。

---

## 1. 🧠 Agent 层：eino

你的选择很明智。eino 的架构分三层，恰好对应 bot 的需求：

```
┌──────────────────────────────────────────────┐
│  adk (Agent Development Kit)                  │
│  ChatModelAgent / DeepAgent / Runner          │
│  负责：ReAct 循环、工具调用决策、多Agent协作      │
├──────────────────────────────────────────────┤
│  compose (编排层)                              │
│  Graph / Chain / Workflow                     │
│  负责：当你需要精确控制流程时用（比如敏感词检测链） │
├──────────────────────────────────────────────┤
│  components (组件抽象)                         │
│  ChatModel / Tool / Retriever / Embedding     │
│  负责：对接 OpenAI/DeepSeek/Ollama 等具体模型    │
└──────────────────────────────────────────────┘
```

**在 bot 中的落点：**

```go
// eino 负责"思考"，ZeroBot 负责"连接"
// 收到消息 → ZeroBot 分发给你的 handler → 你的 handler 调用 eino agent 决策

engine.OnMessage().Handle(func(ctx *ZeroBot.Ctx) {
    // 1. 拼上下文（你是谁、群里在聊什么、之前说过什么）
    systemPrompt := buildSystemPrompt(ctx)
    
    // 2. 交给 eino agent 决策：该不该回复？回复什么？要不要调工具？
    agent := buildAgent(ctx)
    runner := adk.NewRunner(ctx, adk.RunnerConfig{Agent: agent})
    
    // 3. agent 自己决定调工具（发图、查天气、搜网页...）还是直接回复
    for event := range runner.Query(ctx, userMessage) {
        if event.Message != nil {
            ctx.Reply(event.Message.Content)
        }
    }
})
```

**关键决策点：用 ChatModelAgent 还是自己写 ReAct 循环？**

| 方案 | 适合场景 |
|------|---------|
| `adk.NewChatModelAgent` | 标准 tool-calling agent，模型本身支持 function calling |
| `compose.NewGraph` 手写 | 需要精确控制每步（先过滤敏感词 → 再判断意图 → 再决策） |
| DeepAgent | 复杂多步骤任务，需要拆解→分派→汇总 |

**建议：先用 ChatModelAgent + tools 快速跑通，后续复杂流程用 compose.Graph 精确编排。**

---

## 2. 📝 对话存储设计

这是最核心也最容易设计失误的部分。MumuBot 的做法值得参考：

### 三层存储模型

```
┌──────────────────────────────────────────────┐
│  Layer 1: 短期记忆 (Work Memory / 话题记忆)      │
│  当前话题的上下文窗口，直接塞进 prompt              │
│  存储：内存 (LRU 缓存)                           │
│  容量：最近 N 轮对话 / 最近 M 分钟                │
├──────────────────────────────────────────────┤
│  Layer 2: 中期记忆 (Session / 话题归档)          │
│  今天/这几天的聊天摘要，按话题聚类                 │
│  存储：MySQL + Milvus 向量索引                   │
│  用途：跨会话的上下文召回                          │
├──────────────────────────────────────────────┤
│  Layer 3: 长期记忆 (Facts / Profile / Knowledge)│
│  用户画像、群黑话、学到的事实                      │
│  存储：MySQL 结构化 + Milvus 向量检索             │
│  用途：人格一致性、知识积累                        │
└──────────────────────────────────────────────┘
```

### 私聊 vs 群聊的数据模型

```
私聊 (PrivateChat):
  chat_id = "private:{user_id}"
  上下文 = [用户历史消息] + [bot 回复]
  特点：1对1，上下文连续性强

群聊 (GroupChat):
  chat_id = "group:{group_id}"
  上下文 = 
    ├── 群级上下文：群里最近 N 条消息（所有人混在一起）
    ├── 用户级上下文：发言者个人的历史消息（用于理解这个人的风格）
    └── @bot 触发时：拼接「群上下文 + 被@的上下文」
```

```sql
-- 核心表设计
CREATE TABLE messages (
    id          BIGINT PRIMARY KEY,
    chat_type   ENUM('private', 'group') NOT NULL,
    chat_id     VARCHAR(64) NOT NULL,        -- private:12345 或 group:67890
    user_id     BIGINT NOT NULL,             -- 发言者 QQ 号
    content     TEXT NOT NULL,
    role        ENUM('user', 'bot') NOT NULL,
    created_at  DATETIME NOT NULL,
    INDEX idx_chat_time (chat_id, created_at)
);

-- 群聊中，通过 chat_id + user_id 就能区分「群上下文」和「个人上下文」
-- 查群上下文: SELECT * FROM messages WHERE chat_id = 'group:67890' ORDER BY created_at DESC LIMIT 50
-- 查个人上下文: SELECT * FROM messages WHERE chat_id = 'group:67890' AND user_id = 12345 ORDER BY created_at DESC LIMIT 10
```

### prompt 拼接策略（关键！）

```go
func buildPrompt(event *OneBotEvent) string {
    var sb strings.Builder
    
    // 1. 人格设定（不变的）
    sb.WriteString(persona.GetSystemPrompt())
    
    // 2. 群聊氛围（从长期记忆取的群文化）
    sb.WriteString(jargon.GetGroupCulture(event.GroupID))
    
    // 3. 话题上下文（短期记忆，最近 N 条）
    sb.WriteString(fmt.Sprintf("--- 群里最近在聊 ---\n%s\n", 
        topic.GetRecent(event.GroupID, 20)))
    
    // 4. 当前发言者画像（如果 @某人 或 私聊）
    if event.IsPrivate() || event.IsAtBot() {
        sb.WriteString(fmt.Sprintf("--- %s 的画像 ---\n%s\n",
            event.SenderName, profile.Get(event.SenderID)))
    }
    
    // 5. 当前消息
    sb.WriteString(fmt.Sprintf("--- 现在 ---\n%s: %s\n", 
        event.SenderName, event.Message))
    
    return sb.String()
}
```

---

## 3. 🔌 插件设计

你的 bot 核心是 AI agent，所以**插件系统和传统 bot 不太一样**。传统 bot 的插件是「命令 → handler」的映射，而你的插件更像是**给 Agent 注册工具（Tools）**。

### 两种"插件"的概念对比

```
传统 ZeroBot 插件：                   你的 Agent 插件：
┌──────────────────────┐            ┌──────────────────────┐
│ /天气 北京            │            │ 用户: "今天好热啊"      │
│   ↓                  │            │   ↓                  │
│ handler 直接返回结果   │            │ Agent 判断: 需要查天气  │
│ ctx.Reply("25°C")    │            │   ↓                  │
│                      │            │ 调用 weatherTool      │
│ 人类决定调哪个插件      │            │   ↓                  │
│                      │            │ Agent 组装回复         │
│                      │            │ "是啊，北京今天25度..." │
└──────────────────────┘            └──────────────────────┘
```

### 推荐设计：工具注册表 + eino Tool 接口

```go
// 插件 = 实现 eino 的 tool.BaseTool 接口
// 不需要额外的插件框架，eino 本身就是最好的编排层

// 所有工具注册到一张表
var toolRegistry = map[string]tool.BaseTool{
    "weather":     NewWeatherTool(),      // 查天气
    "search_web":  NewWebSearchTool(),    // 搜网页
    "set_remind":  NewReminderTool(),     // 设提醒
    "group_kick":  NewKickTool(),         // 踢人（需要权限检查）
    "send_sticker": NewStickerTool(),     // 发表情包
    "get_profile": NewProfileTool(),      // 查用户画像
}

// Agent 初始化时把所有工具注入
func buildAgent(groupID int64) adk.Agent {
    tools := filterToolsByPermission(groupID) // 按群权限过滤
    return adk.NewChatModelAgent(ctx, &adk.ChatModelAgentConfig{
        Model: chatModel,
        ToolsConfig: adk.ToolsConfig{
            ToolsNodeConfig: compose.ToolsNodeConfig{
                Tools: tools,
            },
        },
    })
}
```

**判断标准：**
- 需要 AI 自主决策调用 → 注册为 eino Tool
- 需要精确触发（如 `/help`）→ 留在 ZeroBot 的 `OnCommand` 层
- **两条路并存，不冲突**

---

## 4. 🤖 AI 如何像真人一样处理事件

这是 MumuBot 做得最精彩的部分。核心思路：**不是每条消息都回复，Agent 自己决定是否介入**。

### MumuBot 的情绪/决策模型

```
┌─────────────────────────────────────────────────────┐
│                    Agent 决策循环                      │
│                                                       │
│  收到消息 ─→ 判断是否要回应 ─→ 是 ─→ 生成回复          │
│                │                    │                 │
│                否                   调用工具?          │
│                │                    │      │          │
│              忽略                  yes     no        │
│                                  │           │       │
│                              执行工具    直接回复      │
│                                  │                   │
│                              收到结果 → 生成回复       │
└─────────────────────────────────────────────────────┘
```

### 关键：什么时候该说话？

```go
// 判断逻辑不是写死的规则，而是让 LLM 自己判断
// prompt 里告诉它：

const judgePrompt = `
你是群聊中的一个成员，不是客服。请根据以下规则判断是否要回复：

必须回复的情况：
- 有人 @ 你（必须回）
- 直接问你问题（"有人知道xxx吗"不算，除非你确实知道）
- 你刚说过相关话题，别人在回应你

应该考虑回复的情况：
- 聊的话题是你的兴趣领域
- 气氛冷场了，可以活跃一下
- 有人说了很有意思的话，可以接梗

应该闭嘴的情况：
- 其他人在激烈讨论，跟你无关
- 你已经连续说了好几轮了
- 只是"嗯""哈哈""草"之类的无意义消息
- 凌晨3点，群里没人说话

你的情绪状态：
- 心情: {mood}/10
- 精力: {energy}/10  
- 社交意愿: {social}/10

最终输出 JSON: {"should_reply": true/false, "reason": "..."}
`
```

### 完整事件处理流水线

```
OneBot 事件
  │
  ├─ 消息事件 (message)
  │   ├─ 命令消息 (/开头) ──→ ZeroBot OnCommand ──→ 直接 handler 回复（不走 AI）
  │   └─ 普通消息 ──→ 情绪判断 ──→ Agent 决策 ──→ 回复/忽略
  │
  ├─ 通知事件 (notice)
  │   ├─ 群成员增加 ──→ 欢迎语（可走 AI 生成个性化欢迎）
  │   ├─ 群成员减少 ──→ 记录日志
  │   ├─ 戳一戳 ──→ 回应（"别戳了！"）
  │   └─ 群禁言/解禁 ──→ 记录日志
  │
  ├─ 请求事件 (request)
  │   ├─ 加好友请求 ──→ 自动通过/拒绝策略
  │   └─ 加群请求 ──→ 自动通过/拒绝策略
  │
  └─ 元事件 (meta_event)
      └─ 心跳 ──→ 记录在线状态
```

### 状态管理：让 AI 有"生命感"

```go
type BotState struct {
    Mood       float64 // 0-10 心情
    Energy     float64 // 0-10 精力
    Socialness float64 // 0-10 社交意愿
    LastActive time.Time
    ChatCount  int     // 今天说了多少句
}

// 状态随时间和事件自然衰减/增长
func (s *BotState) Tick() {
    // 随时间恢复精力
    s.Energy = min(10, s.Energy + 0.01)
    // 心情缓慢回归基线
    s.Mood = s.Mood*0.999 + 5*0.001
}

func (s *BotState) OnPositiveInteraction() {
    s.Mood = min(10, s.Mood + 0.5)
    s.Socialness = min(10, s.Socialness + 0.2)
}

func (s *BotState) OnNegativeInteraction() {
    s.Mood = max(0, s.Mood - 0.3)
}

// 状态影响 prompt 里的数值，进而影响 AI 的回复风格
```

---

## 🎯 整体架构总览

```
┌────────────────────────────────────────────────────────┐
│                    你的 Plumebot                         │
│                                                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │ 命令模块  │  │ 人格模块  │  │ 记忆模块  │  │ 插件工具 │ │
│  │ /命令     │  │ persona  │  │ memory   │  │ tools   │ │
│  │ 直接响应  │  │ 人设管理  │  │ 三层存储  │  │ eino Tool│ │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬────┘ │
│       │             │             │             │        │
│  ┌────┴─────────────┴─────────────┴─────────────┴────┐  │
│  │              Agent 决策核心 (eino)                  │  │
│  │  判断是否说话 → 说什么 → 调什么工具 → 输出          │  │
│  └──────────────────────┬─────────────────────────────┘  │
│                         │                                │
│  ┌──────────────────────┴─────────────────────────────┐  │
│  │           OneBot 连接层 (ZeroBot)                    │  │
│  │  WebSocket ↔ NapCat ↔ QQ                           │  │
│  └─────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

---

这四个层面你还有哪个想深入展开？比如 eino 的具体 tool 注册方式、记忆系统的向量检索方案、或者情绪系统的 prompt 细节？

## User

1.插件不是tool，这个后续可以直接添加，插件就是分发 2.数据存储用sqllite，除了napcat，当前尽量不要添加额外需要下软件的 3.可以设置是@启动，还是ai自己判断，继续讨论

## Assistant

明白了，三个重要约束。让我基于此重新整理设计。

---

## 1. 🔌 插件系统（命令分发）

你的定位很清晰：**插件 = 命令路由，Tool = Agent 能力**。类比：

```
用户说 "/天气 北京"          →  命令插件系统精确匹配 → 直接返回结果
用户说 "今天好热"            →  Agent 判断 → 调 weatherTool → 自然语言回复
```

### ZeroBot 已有的命令能力

ZeroBot 提供了基础的路由原语：

```go
engine.OnCommand("ping").Handle(func(ctx *ZeroBot.Ctx) { ... })       // /ping
engine.OnPrefix("天气").Handle(func(ctx *ZeroBot.Ctx) { ... })        // 天气北京
engine.OnRegex(`^抽卡(\d+)次$`).Handle(func(ctx *ZeroBot.Ctx) { ... }) // 正则
```

### 你需要什么样的插件系统？

ZeroBot 的 `OnCommand` 已经能处理精确命令匹配。你额外需要的是：

| 需求 | ZeroBot 有没有 | 
|------|:---:|
| `/plugin list` 列出所有插件 | ❌ |
| `/enable weather` 在某群启用插件 | ZeroBot-Plugin 有 |
| `/disable weather` 在某群禁用插件 | ZeroBot-Plugin 有 |
| 动态热加载 .so 文件 | ❌ |
| 插件之间设置优先级 | ❌ |

好方案！内建 + exe 外部插件双模式。exe 插件需要一个通信协议。我们来设计方案。

---

## 内建 vs exe 插件的通信模型

```
                          你的 PlumeBot
┌──────────────────────────────────────────────────┐
│                                                    │
│  消息事件 ──→ 路由判断 ──→ 匹配到内建插件 ──→ 直接执行  │
│                  │                                 │
│                  └──→ 匹配到 exe 插件 ──→ IPC 转发   │
│                                            │       │
│                       ┌────────────────────┘       │
│                       │                            │
│              ┌────────┴────────┐                   │
│              │  exe 插件进程     │                   │
│              │  stdin/stdout   │                   │
│              │  JSON 通信      │                   │
│              └─────────────────┘                   │
└──────────────────────────────────────────────────────┘
```

### exe 插件协议设计

最轻量的方案是 **stdio + JSON 行协议**（一行一个 JSON），类似 MCP/LSP：

```
// 主进程 → 插件 (stdin)
{"type":"request","id":"1","command":"天气","args":["北京"],...}
// 插件 → 主进程 (stdout)
{"type":"response","id":"1","content":"北京今天 25°C，晴天"}
```

```go
// 插件接口（内建和 exe 共享同一接口）
type Plugin interface {
    Name() string
    Description() string
    // 匹配规则（命令前缀/正则等）
    Rules() []Rule
    // 执行
    Execute(ctx *Context) (*Reply, error)
}

// 内建插件：直接实现接口
type BuiltinWeatherPlugin struct{}
func (p *BuiltinWeatherPlugin) Execute(ctx *Context) (*Reply, error) { ... }

// exe 插件：通过 proxy 把调用转为 JSON 发给子进程
type ExePlugin struct {
    cmd    *exec.Cmd
    stdin  io.Writer
    stdout *bufio.Scanner
}
func (p *ExePlugin) Execute(ctx *Context) (*Reply, error) {
    p.stdin.Write(jsonReq)  // 发给插件进程
    return p.stdout.Scan()   // 读取回复
}
```

### exe 插件的生命周期

```go
type ExePluginManager struct {
    plugins map[string]*ExePlugin
}

// 扫描 plugins/ 目录，发现 .exe 文件
func (m *ExePluginManager) Discover(dir string) {
    // plugins/weather.exe → 启动子进程 → 握手 → 注册
    // plugins/music.exe   → 启动子进程 → 握手 → 注册
}

// 健康检查：定期 ping 子进程，挂了就重启
func (m *ExePluginManager) HealthCheck() {
    for _, p := range m.plugins {
        if !p.Alive() {
            p.Restart()
        }
    }
}
```

### 插件管理命令

```
/plugin list                    → 列出所有插件及其状态
/plugin enable weather          → 在当前群启用 weather 插件
/plugin disable weather         → 在当前群禁用 weather 插件
/plugin info weather            → 查看插件详情
```

不管内建还是 exe，**管理命令对用户透明**。

---

## 2. 💾 SQLite 存储方案

SQLite 的问题是：**向量检索做不了**。但我们可以分层降级：

### 不用向量检索，用务实方案

```
记忆类型            │ 存储方式                    │ 检索方式
────────────────────┼───────────────────────────┼─────────────────
短期（当前话题）      │ 内存 ring buffer            │ 直接取最近 N 条
话题归档             │ SQLite topic_summary 表     │ 关键词 LIKE + 时间排序
群友画像             │ SQLite user_profile 表      │ 精确查 user_id
群文化/黑话          │ SQLite jargon 表            │ 精确查 keyword
消息日志             │ SQLite messages 表          │ 按 chat_id + 时间
```

```sql
-- 消息表
CREATE TABLE messages (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    chat_type  TEXT NOT NULL,        -- 'private' / 'group'
    chat_id    TEXT NOT NULL,        -- 'group:12345' / 'private:67890'
    sender_id  INTEGER NOT NULL,
    sender_name TEXT,
    content    TEXT NOT NULL,
    msg_type   TEXT DEFAULT 'text',  -- text/image/at/sticker
    created_at TEXT NOT NULL         -- ISO8601
);
CREATE INDEX idx_msg_chat_time ON messages(chat_id, created_at);

-- 群友画像
CREATE TABLE user_profile (
    user_id    INTEGER PRIMARY KEY,
    nickname   TEXT,
    tags       TEXT,                 -- JSON array: ["宅男","爱发猫图","夜猫子"]
    facts      TEXT,                 -- JSON: {"生日":"1月1日","爱好":"打游戏"}
    first_seen TEXT,
    last_seen  TEXT
);

-- 话题摘要（归档用）
CREATE TABLE topic_summary (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    chat_id    TEXT NOT NULL,
    topic      TEXT NOT NULL,        -- 关键词摘要
    summary    TEXT,                 -- LLM 生成的摘要
    start_time TEXT,
    end_time   TEXT
);

-- 群文化/黑话
CREATE TABLE jargon (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    chat_id    TEXT NOT NULL,
    keyword    TEXT NOT NULL,        -- "牛牛"
    meaning    TEXT,                 -- "指战斗力/伤害值"
    created_at TEXT
);

-- 插件配置（每个群）
CREATE TABLE plugin_config (
    chat_id    TEXT NOT NULL,
    plugin_name TEXT NOT NULL,
    enabled    INTEGER DEFAULT 1,
    config     TEXT,                 -- JSON 配置
    PRIMARY KEY (chat_id, plugin_name)
);

-- 群全局设置
CREATE TABLE group_config (
    chat_id    TEXT PRIMARY KEY,
    trigger_mode TEXT DEFAULT 'mention',  -- 'mention' / 'auto'
    command_prefix TEXT DEFAULT '/',
    ai_enabled INTEGER DEFAULT 1,
    config     TEXT                  -- JSON 额外配置
);
```

### 如果以后想要向量检索？

SQLite 有一个扩展 [sqlite-vec](https://github.com/asg017/sqlite-vec)，不需要外部服务，纯 C 动态库。可以后续接入：

```sql
-- 用 sqlite-vec 扩展
CREATE VIRTUAL TABLE msg_vectors USING vec0(
    embedding float[768]
);
-- 检索
SELECT * FROM msg_vectors 
WHERE embedding MATCH (SELECT embedding FROM ... WHERE id = ?) 
LIMIT 10;
```

但**一开始不需要**，关键词 + 时间排序对 bot 场景够了。真正需要精确上下文召回的是 AI 对话，那条路用「最近 N 条消息直接塞 prompt」就够了。

---

## 3. 🎯 触发模式设计

两种模式，可配置：

```
mention 模式：只有被 @ 或私聊才说话
  ┌─────┐
  │ @bot │──→ 一定回复
  └─────┘
  ┌─────────────┐
  │ 群普通消息     │──→ 忽略
  └─────────────┘
  ┌─────────┐
  │ 私聊消息  │──→ 一定回复
  └─────────┘

auto 模式：AI 自主判断是否加入对话
  ┌─────────────┐
  │ @bot / 私聊  │──→ 一定回复（优先级最高）
  └─────────────┘
  ┌─────────────┐      ┌──────────────┐
  │ 群普通消息     │──→  │ Agent 判断    │──→ 回复/忽略
  └─────────────┘      │ (情绪+上下文)  │
                       └──────────────┘
```

```go
type TriggerMode string

const (
    TriggerMention TriggerMode = "mention"  // 仅被@时回复
    TriggerAuto    TriggerMode = "auto"     // AI 自主判断
)

type EventHandler struct {
    agent   adk.Agent
    state   *BotState
}

func (h *EventHandler) OnGroupMessage(ctx *Ctx) {
    mode := config.GetTriggerMode(ctx.GroupID)
    
    switch mode {
    case TriggerMention:
        if ctx.IsAtBot() || ctx.IsCommand() {
            h.dispatch(ctx)
        }
        // 否则静默忽略
        
    case TriggerAuto:
        if ctx.IsAtBot() || ctx.IsCommand() {
            h.dispatch(ctx)  // @ 一定回复
            return
        }
        // AI 判断是否接话
        if h.shouldJoin(ctx) {
            h.dispatch(ctx)
        }
    }
}
```

### AI 自主判断的逻辑（auto 模式）

不是每条消息都调 LLM 判断（那样太贵），可以加预过滤器：

```go
func (h *EventHandler) shouldJoin(ctx *Ctx) bool {
    // 预过滤器（规则层，不调 LLM，零成本）
    // 1. 太短的消息不接（"嗯""哈哈""草"）
    if len(ctx.Message) < 4 {
        return false
    }
    // 2. 精力太低不说话
    if h.state.Energy < 3 {
        return false
    }
    // 3. 连续说了太多轮，休息一下
    if h.state.ConsecutiveReply >= 5 {
        h.state.ConsecutiveReply = 0
        return false
    }
    // 4. 凌晨不接话（除非有活人在聊）
    if h.isNight() && h.chatActivity(ctx.GroupID) < 3 {
        return false
    }
    
    // 通过预过滤后，再调轻量 LLM 判断
    result := h.agent.Judge(ctx)
    return result.ShouldReply
}
```

预过滤能挡掉 80% 的消息，只有少数消息才真正调 LLM 做判断。

---

## 📊 整体架构更新版

```
PostgreSQL/MySQL: ❌  →  SQLite  ✅
Milvus:          ❌  →  关键词+时间检索 ✅
Redis:           ❌  →  Go内存+SQLite ✅

除了 NapCat，零额外依赖！
```

```
plumebot/
├── main.go                      # 入口，启动 ZeroBot
├── config/
│   └── config.yaml              # 全局配置
├── internal/
│   ├── bot/                     # bot 主逻辑
│   │   ├── handler.go           # 事件处理（触发模式判断）
│   │   └── state.go             # 情绪/状态管理
│   ├── agent/                   # Agent 层 (eino)
│   │   ├── agent.go             # ChatModelAgent 初始化
│   │   ├── prompt.go            # prompt 构建
│   │   └── tools/               # Agent 工具
│   │       ├── weather.go
│   │       └── search.go
│   ├── plugin/                  # 插件系统
│   │   ├── manager.go           # 注册、发现、管理
│   │   ├── interface.go         # Plugin 接口定义
│   │   ├── exe_proxy.go         # exe 插件代理（子进程管理）
│   │   └── builtin/             # 内建插件
│   │       ├── help.go
│   │       └── admin.go
│   ├── storage/                 # SQLite 层
│   │   ├── db.go                # 连接管理
│   │   ├── messages.go
│   │   ├── profile.go
│   │   ├── topic.go
│   │   └── config.go
│   ├── persona/                 # 人格模块
│   │   └── persona.go
│   └── onebot/                  # ZeroBot 封装
│       └── engine.go
├── plugins/                     # 外部 exe 插件存放目录
│   └── (用户放的 .exe 文件)
└── data/                        # SQLite 数据库文件
    └── plumebot.db
```

---

还有一些可以深入的点：
- **onebot 层的封装**：ZeroBot 怎么包装成我们自己的接口，方便以后换 SDK？
- **exe 插件的握手协议细节**：怎么发现、怎么注册路由、怎么心跳？
- **prompt 管理**：人格 prompt 怎么设计模板、怎么热更新？

你想先聊哪个？

## User

exe插件实现不要用网络通信，而是命令行工具一样，通过指令实现

## Assistant

明白了，纯 stdio 通信，每次调用像跑命令行一样。这样设计最简洁：

---

## exe 插件：命令行模式设计

```
主进程调用插件就像执行命令：

  echo '{"command":"天气","args":["北京"]}' | ./plugins/weather.exe
  → {"ok":true,"data":"北京今天 25°C"}

对比传统命令行：
  ./plugins/weather.exe 北京
  → 北京今天 25°C

区别：用 JSON 协议而不是裸参数，可以传复杂结构（消息上下文、用户信息等）
```

### 协议定义

```go
// 主进程 → 插件 (stdin)  — 每一行是一个完整的 JSON
type PluginRequest struct {
    ID      string          `json:"id"`      // 请求ID，用于日志追踪
    Command string          `json:"command"` // 匹配到的指令名
    Args    []string        `json:"args"`    // 指令参数
    Context PluginContext   `json:"context"` // 消息上下文
}

type PluginContext struct {
    ChatType   string `json:"chat_type"`   // "group" / "private"
    ChatID     string `json:"chat_id"`
    SenderID   int64  `json:"sender_id"`
    SenderName string `json:"sender_name"`
    Message    string `json:"message"`     // 原始消息文本
    IsAdmin    bool   `json:"is_admin"`
}

// 插件 → 主进程 (stdout)  — 一行 JSON
type PluginResponse struct {
    ID      string          `json:"id"`      // 对应请求 ID
    OK      bool            `json:"ok"`
    Error   string          `json:"error,omitempty"`
    Content string          `json:"content"` // 回复文本
    Image   string          `json:"image,omitempty"`  // 可选图片路径/base64
    At      bool            `json:"at"`      // 是否@发送者
}
```

### 插件的两种生命周期模式

```
模式A：一次性（默认，推荐）
┌────────┐     start+stdin     ┌─────────┐
│ 主进程  │ ──────────────────→ │ 插件.exe │
│        │ ←──── stdout ────── │         │
│        │     进程退出          └─────────┘
└────────┘
适用：大部分插件，调用频率低

模式B：常驻（可选）
┌────────┐     start            ┌─────────┐
│ 主进程  │ ──────────────────→  │ 插件.exe │
│        │ ←── 握手确认 ─────── │         │
│        │ ═══ stdin 多条请求 ══→│         │    ← 一条请求一行 JSON
│        │ ←══ stdout 多条响应 ══│         │    ← 一条响应一行 JSON
│        │      ...             │         │
└────────┘                      └─────────┘
适用：高频调用、需要内存缓存的插件
```

```go
// 插件声明自己是哪种模式（在插件目录下放一个 plugin.json 或者约定信号）
// plugins/weather/
//   ├── plugin.json    ← 元数据
//   └── weather.exe    ← 可执行文件

// plugin.json
{
    "name": "weather",
    "version": "1.0",
    "description": "查天气",
    "mode": "oneshot",           // "oneshot" 或 "daemon"
    "commands": ["天气", "weather"],
    "timeout_ms": 5000
}
```

### Go 侧执行逻辑

```go
type ExePlugin struct {
    Info     PluginMeta
    ExePath  string
    // daemon 模式才用下面两个
    cmd      *exec.Cmd
    stdin    io.WriteCloser
    stdout   *bufio.Scanner
}

func (p *ExePlugin) Execute(ctx *PluginContext) (*PluginResponse, error) {
    switch p.Info.Mode {
    case "oneshot":
        return p.executeOneshot(ctx)
    case "daemon":
        return p.executeDaemon(ctx)
    }
}

// 一次性模式：启动 → 写请求 → 读响应 → 进程退出
func (p *ExePlugin) executeOneshot(ctx *PluginContext) (*PluginResponse, error) {
    cmd := exec.Command(p.ExePath)
    cmd.Stderr = os.Stderr // 插件自己的日志打到主进程 stderr
    
    stdin, _ := cmd.StdinPipe()
    stdout, _ := cmd.StdoutPipe()
    
    cmd.Start()
    
    // 发请求
    req := PluginRequest{
        ID:      uuid.New().String(),
        Command: ctx.MatchedCommand,
        Args:    ctx.Args,
        Context: *ctx,
    }
    json.NewEncoder(stdin).Encode(req)
    stdin.Close() // 告诉插件：输入结束了
    
    // 读响应（设置超时）
    var resp PluginResponse
    ch := make(chan error, 1)
    go func() {
        ch <- json.NewDecoder(stdout).Decode(&resp)
    }()
    
    select {
    case err := <-ch:
        if err != nil {
            return nil, err
        }
    case <-time.After(time.Duration(p.Info.TimeoutMs) * time.Millisecond):
        cmd.Process.Kill()
        return nil, errors.New("plugin timeout")
    }
    
    cmd.Wait()
    return &resp, nil
}
```

### 插件开发者体验

一个 weather 插件，可以是任意语言编写：

```go
// Go 写的插件
func main() {
    var req PluginRequest
    json.NewDecoder(os.Stdin).Decode(&req)
    
    city := req.Args[0]
    result := queryWeather(city) // 你只管实现业务
    
    resp := PluginResponse{
        ID:      req.ID,
        OK:      true,
        Content: fmt.Sprintf("%s今天 %s，%d°C", city, result.Weather, result.Temp),
    }
    json.NewEncoder(os.Stdout).Encode(resp)
}
```

```python
# Python 写的插件
import sys, json

req = json.loads(sys.stdin.readline())
city = req["args"][0]
result = query_weather(city)

resp = {"id": req["id"], "ok": True, "content": f"{city}今天晴天，25°C"}
print(json.dumps(resp))
```

**就是读 stdin 一行 JSON，写 stdout 一行 JSON。任意语言，零依赖。**

---

### 插件发现与注册

```
启动时扫描 plugins/ 目录：

plugins/
├── weather/
│   ├── plugin.json        ← 有 plugin.json → 结构化注册
│   └── weather.exe
├── music.exe              ← 无 plugin.json → 用默认规则
└── translate.py           ← 也可以（不限于 .exe，任何可执行文件）
```

```go
func (m *PluginManager) Discover(dir string) error {
    entries, _ := os.ReadDir(dir)
    
    for _, entry := range entries {
        pluginDir := filepath.Join(dir, entry.Name())
        
        // 优先读 plugin.json
        meta, err := readPluginJSON(pluginDir)
        if err != nil {
            // 没有 json，用文件名作为 name，默认 oneshot
            meta = PluginMeta{
                Name:     strings.TrimSuffix(entry.Name(), filepath.Ext(entry.Name())),
                Mode:     "oneshot",
                Commands: []string{entry.Name()},
                TimeoutMs: 5000,
            }
        }
        
        // 找到可执行文件
        exePath := findExeInDir(pluginDir)
        
        m.Register(&ExePlugin{Info: meta, ExePath: exePath})
    }
}
```

---

### 汇总：三种插件形态统一接口

```go
type Plugin interface {
    Meta() PluginMeta
    Execute(cmd string, args []string, ctx *PluginContext) (*PluginResponse, error)
}

// 1. 内建插件 — 直接实现接口，编译进二进制
type BuiltinPlugin struct { ... }

// 2. exe 一次性插件 — fork 子进程跑完退出
type ExeOneShotPlugin struct { ExePath string; ... }

// 3. exe 常驻插件 — fork 后保持通信
type ExeDaemonPlugin struct { cmd *exec.Cmd; ... }
```

对主程序来说，三者完全一样，`plugin.Execute()` 无需区分。

---

接下来你想讨论哪个方向？
- **onebot 层的封装**（ZeroBot → 我们自己的接口）
- **prompt / 人格模板的设计**
- 还是其他？

## User

不要注重代码实现细节，现在讨论的是架构

## Assistant

你说得对，我偏实现了。回到架构层。

---

## 刚才已定下的架构决策

| 决策 | 结论 |
|------|------|
| OneBot 连接层 | ZeroBot |
| Agent 引擎 | eino (CloudWeGo) |
| 存储 | SQLite，零额外服务 |
| 外部依赖 | 仅 NapCat |
| 插件形态 | 内建（编译进二进制）+ exe（子进程 stdio JSON） |
| 触发模式 | mention / auto 可配置 |
| 命令 vs Agent | 命令走插件分发，AI 对话走 Agent |

---

接下来要看的是**没有敲定的架构问题**。你觉得这几个方向哪个优先级高？

1. **整体分层**：ZeroBot、eino、插件系统、存储这几块之间的边界和依赖方向
2. **OneBot 层要不要封装**：是直接用 ZeroBot 的 API，还是包一层自己的接口（为将来换实现端留后路）
3. **记忆/上下文架构**：三层记忆怎么分工、Agent 调用记忆的流程
4. **人格系统**：prompt 模板、人设热更新、多群不同人格
5. **事件管线的整体流程**：从收到 OneBot 事件到最终回复，经过哪些处理节点

你关心哪个？

## User

exe插件使用go的exec，存储的向量检索搜索有没有轻量的框架，群聊记忆分为群聊个人画像，群聊画像，最近上下文窗口

## Assistant

好的，我搜索一下 Go 生态中轻量的嵌入向量检索方案，同时梳理记忆架构。

好的。先说向量检索，再说记忆架构。

---

## 向量检索：方案对比

| 方案 | 需要装的服务 | 需要 CGO | 成熟度 |
|------|:---:|:---:|------|
| **sqlite-vec** | ❌ 无 | ✅ 需要 | 7.9k stars，Mozilla 赞助 |
| 纯 Go 内存暴力检索 | ❌ 无 | ❌ 无 | 自己写，数据量小完全够 |
| Milvus / Qdrant | ✅ 要装 | — | 开坦克抓蚊子 |

**建议：起步不搞向量检索。** 先用 SQLite 的关键词 + 时间排序召回，路径最短。如果后面发现语义搜索是刚需，加 sqlite-vec 只是改 SQL 语句——它就是一个 SQLite 扩展，不引入新进程。

---

## 记忆三层架构

你提的三层分法很清晰，逐层看：

### 第一层：最近上下文窗口

```
本质：纯内存，不落盘
容量：最近 N 条消息（比如 20~50 条）
生命周期：bot 进程期间，重启丢失（无所谓，只是窗口滑动）
作用：拼进 prompt，让 Agent 知道"群里刚刚在聊什么"
```

**不需要 SQLite**，就是一个 ring buffer。这条链路最轻。

---

### 第二层：群聊个人画像

```
粒度：某个人在某个群里的表现
存储：SQLite，按 (group_id, user_id) 唯一
内容：
  - 昵称、QQ号（不变）
  - 发言风格标签：话痨/潜水/爱发图/杠精/复读机...
  - 活跃度统计：发言次数、最后活跃时间、历史时间分布
  - 亲密度：与 bot 的互动频率
  - 事实性记忆："他说过他是程序员""他养了一只猫"
  - 最近兴趣话题（从发言中提取）
更新方式：渐进式更新，不是每次都重算
召回方式：精确查询 user_id
```

**为什么放这里而不是全局用户画像？** 同一个人在不同群表现可能完全不同——在公司群正经，在水群放飞自我。画像绑定到群才有意义。

---

### 第三层：群聊画像

```
粒度：每个群一个
存储：SQLite
内容：
  - 群名、群号（不变）
  - 群文化特征：主流话题、活跃时段、群规
  - 群黑话词典：{ "牛牛": "战斗力", "出货": "抽到好东西" }
  - 群氛围标签：技术群/水群/游戏群/...
  - 群成员关系网络：谁和谁经常互动
  - bot 自己在这个群的人设（可以有微调）
更新方式：
  - 黑话/梗：持续学习，人工审核
  - 氛围标签：周期性（每天/每周）用 LLM 分析聊天摘要
  - 活跃时段：纯统计
召回方式：精确查询 group_id
```

---

### 三层协作流

```
消息进来时的处理流：

Agent 收到消息：
  │
  ├─→ [窗口] 取最近 N 条消息 ────────────────────────→ 拼进 prompt（知道在聊什么）
  │
  ├─→ [个人画像] 查发言人画像 ───────────────────────→ 拼进 prompt（知道对方是谁）
  │     └─ 查其他被 @ 的人的画像（如果有）
  │
  ├─→ [群画像] 查群文化/黑话 ────────────────────────→ 拼进 prompt（知道怎么说话）
  │     └─ 如果是 mention 模式，只查 bot 自己在群里的人设
  │
  └─→ 组装成完整 prompt → 送给 eino Agent 决策
```

**反馈闭环**：Agent 说完话后，不只有回复文本。它还可以输出"记忆更新指令"：

```
Agent 输出：
  reply: "你上次说要学 Rust，现在进度怎么样了？"
  memory_ops:
    - store_fact: { user: 123, fact: "正在学 Rust" }
    - update_profile: { user: 123, tag: "程序员" }
    - learn_jargon:  { word: "寄",    meaning: "完蛋了/G了" }
```

这样 Agent 自己驱动记忆更新，而不是另外跑一套分析管道。

---

### SQLite 表结构（只讲关系，不讲 SQL）

```
messages          — 消息日志，所有消息落盘
group_profile     — 群画像（1 群 1 条）
group_jargon      — 群黑话（1 群 N 条，belongs_to group_profile）
member_profile    — 群内个人画像（1 人 1 群 1 条，唯一键 group_id + user_id）
member_facts      — 个人事实记忆（1 人 N 条，belongs_to member_profile）
bot_state         — bot 在各群的状态（触发模式、情绪值等）
plugin_config     — 插件在各群的配置
```

**表之间不搞外键约束，只靠逻辑归属**——轻量，不伤性能。

---

## 当前架构全景

```
                   NapCat (QQ登录)
                        │
                   OneBot WebSocket
                        │
                   ┌────┴────┐
                   │ ZeroBot  │  ← 连接层，只管收发
                   └────┬────┘
                        │ 事件
                   ┌────┴────┐
                   │  路由层  │  ← 命令/普通消息/@消息 分流
                   └──┬───┬──┘
                      │   │
              ┌───────┘   └───────┐
              ▼                   ▼
        ┌──────────┐       ┌──────────────┐
        │ 插件系统  │       │  Agent 决策    │
        │ 命令分发  │       │ (eino 引擎)   │
        │ /xxx    │       │ 是否说话/说什么 │
        └──────────┘       └──────┬───────┘
                                  │
                        ┌─────────┼─────────┐
                        ▼         ▼         ▼
                    [窗口]    [个人画像]   [群画像]
                    内存       SQLite     SQLite
```

---

接下来你想深入哪个方向？比如：

- **Agent 的记忆更新闭环**（Agent 如何驱动画像更新）
- **多群 bot 人设隔离**（同一个 bot 在不同群风格不同）
- **OneBot 事件类型的完整覆盖**（哪些事件要拦截、哪些可忽略）
- 还是我们开始搭建项目骨架？

## User

上下文窗口采用动态模式，比如设置初始为20轮，上限为100轮，则随对话不断累加，达到上线后压缩（压缩条数不计入），当压缩条数达到一定程度后，在对其除了，将当前对话计划写入docs文件中（markdown）防止后续讨论忘记细节

## Assistant

这个设计把记忆管理提升了一个档次。让我把整个管道梳理清楚。

---

## 窗口生命周期

```
初始状态：
┌────────────────────────┐
│ 窗口 (20轮空位)         │  最大容量: 100 轮
│ 当前轮数: 0             │
└────────────────────────┘

增长中：
┌────────────────────────┐
│ msg1  msg2  msg3 ...   │  轮数: 50
│ 还在初始容量以内         │  无事发生
└────────────────────────┘

达到上限：
┌────────────────────────────────────────────┐
│ msg1 ... msg80  │  msg81 ... msg100  │
│ ← 最早的 80 轮   │  ← 保留的 20 轮     │
│      ↓ 压缩       │                    │
│  [摘要1]          │                    │
└──────────────────┴────────────────────┘
  摘要不计入轮数        继续对话，窗口又回到 20 轮可用

继续增长 → 再次满 100 → 再次压缩 → [摘要2]

多个摘要累积：
┌────────┬────────┬────────┬──────────────────┐
│[摘要1]  │[摘要2]  │[摘要3]  │ msg_last_20 ...    │
│ 5轮     │ 8轮     │ 6轮     │ (当前窗口)          │
└────────┴────────┴────────┴──────────────────┘
          ↑ 摘要累计过多时触发二次压缩

二次压缩：
┌────────────────────────┬─────────────┐
│ [综合摘要 v1]            │ msg_last_20 │
│ (摘要1+2+3 再压缩)       │ 当前窗口     │
└────────────────────────┴─────────────┘
          ↓ 同时写入文件

归档：
data/chat/group_12345/
├── summary_2026-07-27_v1.md    ← 综合摘要归档
├── summary_2026-07-28_v1.md
└── plan_active.md              ← 当前对话计划（活跃维护）
```

---

## 关键架构决策点

有几个需要讨论的：

前两点已经清晰了，先整理。

---

## 已明确的架构

### 一、压缩策略

```
窗口 100 轮满 → 触发压缩：

┌──────────────────────────────────────────┐
│ msg1 ... msg80  │  msg81 ... msg100      │
│                 │                        │
│    → LLM 压缩 → │  保留，继续对话           │
│                 │                        │
│  输出:           │                        │
│  {               │                        │
│    "summary": "大家讨论了周末爬山的计划...",  │
│    "keywords": ["爬山","周末","装备","路线"], │
│    "decisions": ["路线选A口","周日8点出发"]   │
│  }               │                        │
└──────────────────────────────────────────┘
```

摘要存入 SQLite，带关键词。后续多段摘要再二次压缩：多条摘要 → LLM 融合 → 一条综合摘要。

### 二、Prompt 组装顺序

```
┌──────────────────────┐
│ ① 系统人格            │  ← 固定的，"你是谁"
├──────────────────────┤
│ ② 群聊画像            │  ← 群文化/黑话
├──────────────────────┤
│ ③ 压缩摘要            │  ← 历史上下文（全量拼接）
│   - 综合摘要 v1        │
│   - 综合摘要 v2        │
├──────────────────────┤
│ ④ 当前窗口            │  ← 最近 N 轮原始消息
├──────────────────────┤
│ ⑤ 当前消息            │  ← 正在处理的这条
└──────────────────────┘
```

Agent 的视角是：**先知道我是谁和群文化，再回顾之前聊过什么，再看最近在聊什么，最后处理当前消息。**

### 三、对话计划归档

这一点我没完全理解你的意思。你说的"将当前对话计划写入 docs/xxx.md"是指：

明白了，把我们现在讨论的架构设计归档到项目 `docs/` 目录，作为项目知识沉淀。这个可以先做。

不过在那之前，架构还有几个缺口没聊到，我想确认是先继续讨论完再归档，还是：

- **先归档当前已讨论的部分**，后续新讨论的内容增量追加到 `docs/` 里？
- 还是**继续聊，最后一次性归档**？

另外还有个核心问题没涉及——**OneBot 层的封装策略。** 目前定的是用 ZeroBot，但你是直接暴露出 ZeroBot 原生 API 给上层用，还是在 ZeroBot 之上包一层自己的抽象（目的是将来可以换 NapCat 以外的实现端，比如 LLOneBot、OpenShamrock 等）？

你怎么看？

## User

具体接口实现后续了解

## Assistant

明白了。那我们现在可以收拢，把这段时间讨论的架构决策做一次总结，后续自然就进入搭建阶段了。

我先把当前讨论成果整理出来，然后确认下一步方向。

---

## 📋 当前架构决策汇总

| # | 决策点 | 结论 |
|---|--------|------|
| 1 | OneBot 连接层 | ZeroBot |
| 2 | Agent 引擎 | eino (CloudWeGo) |
| 3 | 存储 | SQLite，零额外服务 |
| 4 | 外部依赖 | 仅 NapCat |
| 5 | 插件形态 | 内建（编译进二进制）+ exe（子进程 stdio JSON） |
| 6 | 触发模式 | mention / auto 可配置 |
| 7 | 命令 vs Agent | 命令走插件分发，AI 对话走 Agent |
| 8 | 记忆三层 | 动态窗口 + 群聊个人画像 + 群聊画像 |
| 9 | 窗口压缩 | 100 轮上限 → AI 摘要 + 关键词 → 多摘要再压缩 |
| 10 | Prompt 顺序 | 人格 → 群画像 → 压缩摘要 → 窗口 → 当前消息 |
| 11 | 向量检索 | 暂不引入，先做关键词检索 |
| 12 | OneBot 封装 | 具体接口实现时再定 |

**尚未讨论的方向：**
- 人格系统（多群人设隔离、模板热更新）
- 事件处理管线（完整事件覆盖清单）
- Agent 记忆更新闭环
- 情绪/状态系统
- 安全与风控

---

下一步你想怎么走？继续聊架构细节，还是开始搭项目骨架让 bot 先跑起来？

## User

先写入文件防止丢失细节

## Assistant

好的，把这段时间讨论的所有架构决策落盘。

已写入 `docs/architecture.md`，所有讨论的架构决策都在里面了。

接下来继续聊架构细节，还是开始搭项目骨架让 bot 先跑起来？

## User

移除活跃对话计划，综合摘要归档

## Assistant

已移除活跃对话计划，综合摘要归档保留为纯文件归档。文档已更新。

## User

9.2移除

## Assistant

还有一个多余的代码块标记残留，清理掉。

已移除 9.2 节。9 节现在只保留数据库表概要，干净了。

## User

sqlite还需存储整个群聊的对话供关键词检索或向量检索

## Assistant

这个其实和 `messages` 表重叠，但文档里没把检索用途说清楚。我补充明确。

同时把 4.1 节也补一句，窗口和 SQLite 的关系。

已更新。现在 4.1 和 9.1 都明确了：全量消息落 SQLite，当前支持关键词检索，后续可扩展向量检索。

## User

当压缩摘要也达到一定长度时逐步舍弃

## Assistant

原 5.3 也需要重编号。

完整生命周期已闭环：

```
窗口满 → 一级压缩(摘要) → 摘要累积 → 二级压缩(综合摘要+归档)
                                            │
                                    综合摘要也满 → 淘汰最旧的（文件已存）
```

文档已更新。还有要调的么？

## User

丢弃的摘要直接舍弃即可

## Assistant

简洁了。继续？

## User

如果是@才启动，如何让ai知道之前的消息呢

## Assistant

好问题。核心是：**"不回复 ≠ 不看"**。

即使 mention 模式不触发回复，消息事件照样流入 bot：

```
mention 模式下：
  消息进 bot ──→ 写入窗口（ring buffer）──→ 写入 SQLite
                        │
                   判断是否 @bot？
                    │        │
                   是        否
                    │        │
                 回复      丢弃（但消息已缓存）
```

被 @ 时，窗口里已经缓存了最近的上下文，Agent 自然就知道群里在聊什么。

之前文档里"触发模式"那节只说了回复策略，没提消息始终会缓存。补上这个逻辑。

已补上。核心原则一句话：**bot 永远是群里的静默听众，只是 decide 要不要开口。**

## User

如果程序重启，丢失了上下文，重启只能读取画像怎么办

## Assistant

重启后恢复链路现在是完整的：

```
正常运行时：消息 → 窗口（内存） + SQLite（持久）
重启时：    SQLite → 拉取最近 N 条 → 窗口重建
任何时刻：  画像始终在 SQLite，不受重启影响
```

## User

不要从SQLite message重建窗口，正常运行即可，对bot未接收的消息通过tool查询qq群聊历史记录（napcat是否支持）

## Assistant

先查 NapCat 是否支持历史消息 API。

基于 OneBot v11 标准，NapCat 支持 `get_group_msg_history`（按 `group_id` + `message_seq` 拉历史消息）。**支持。**

更新文档：

已更新。逻辑变为：

```
正常运行时：消息自然流入窗口，积累上下文
重启后：    窗口从零开始，旁听几轮就恢复了
缺口补齐：  如果被 @ 时发现上下文不够，Agent 调 get_group_msg_history tool
           从 NapCat 拉群聊历史补全
```

这样 Agent 有了主动"翻聊天记录"的能力，而不是被动等消息。继续？

## User

翻阅聊天记录是否会返回所有消息，是否支持关键词

## Assistant

基于 OneBot v11 标准，`get_group_msg_history` 是按序号分段拉取，不支持关键词搜索。

于是在架构上形成互补：

```
                 NapCat API                    SQLite messages 表
              ──────────────                  ──────────────────
拉取方式      按 message_seq 分段              按 chat_id + 时间 + 关键词
用途         补齐 bot 离线期间的缺口             bot 在线时旁听积累的全量数据
搜索能力      ❌ 无关键词                         ✅ LIKE / 全文检索
```

更新文档，把这点说清楚。

同时把 2 节里向量检索那行也更新一下，把本地检索能力说透。

总结一下两块的分工：

| 能力 | SQLite messages | NapCat API |
|------|:---:|:---:|
| 数据来源 | bot 在线旁听积累 | 离线期间的消息 |
| 关键词搜索 | ✅ | ❌ |
| 按时间范围拉取 | ✅ | ❌ |
| 按序号拉取 | — | ✅ |

Agent 的"翻聊天记录" Tool 逻辑：**先查本地 SQLite，不够再调 NapCat API 补全。** 各自负责自己擅长的部分。

## User

按序号拉取是什么序号

## Assistant

就是 QQ 群里的**消息编号**，从建群开始每发一条消息就 +1，全群唯一且连续递增。不是 message_id（那个是全局 ID）。

```
群里每发一条消息，seq 就长一位：

  seq 10001:  张三：有人吗？
  seq 10002:  李四：有
  seq 10003:  张三：今天中午吃啥
  seq 10004:  王五：食堂
  ...

bot 收事件时能拿到 seq → 记住最后收到的 seq
重启后 → 从上次的 seq+1 开始拉 → 精准补全断档
```

不需要理解 message_id 那种随机字符串，seq 就是自然序号。

## User

tool设计未 bot 收事件时能拿到 seq → 记住最后收到的 seq
  │ 重启后 → 从上次的 seq+1 开始拉 → 精准补全断档

## Assistant

在 4.1 节把 seq 补全的机制写清楚。

已更新。这套 Tool 设计现在很精准：seq 就像书签，重启后翻到上次读到的那页继续。

## User

拉取后应写入sql

## Assistant

现在链路完整了：

```
重启 → seq+1 → NapCat API 拉取 → 写入 SQLite + 填入窗口
                                       │
                                  后续可关键词检索
```

## User

继续讨论

## Assistant

好，当前待讨论的还有这些：

1. **人格系统** — 多群人设隔离、模板热更新
2. **事件处理管线** — 完整 OneBot 事件覆盖清单
3. **Agent 记忆更新闭环** — 画像如何自动维护
4. **情绪/状态系统** — bot 的生命感
5. **安全与风控** — 防封号、敏感内容过滤

你想先聊哪个？

## User

1

## Assistant

人格系统需要解决三个核心问题。

---

## 人格 = 模板文件（可热更新）+ 群级绑定

```
                               SQLite group_config
persona/default.yaml  ←──────── persona_template: "default"
persona/tech.yaml     ←──────── group 67890: "tech"
persona/anime.yaml    ←──────── group 12345: "anime"
```

每个群绑定一个模板，未绑定的群用默认。模板之间可以有 `extends` 继承关系。

### 模板结构设计

```yaml
name: "技术群模式"
extends: default          # 继承默认，只覆写差异部分
description: "适合技术讨论群"

# prompt 里直接用的部分
persona: |
  你是一个混迹技术群的赛博群友。
  说话随意但专业，爱用梗，偶尔毒舌。
  ...

traits:                   # 性格标签（影响回复风格）
  - 技术宅
  - 毒舌
  - 爱玩梗

tone: "随意，允许吐槽"     # 语气基调

constraints:               # 硬约束
  - 不回答政治敏感问题
  - 不解锁、不越狱

examples:                  # few-shot 示例
  - user: "这代码写得怎么样？"
    bot: "能跑就行，别追求优雅了（狗头）"
```

### 热更新

```
方案A：fsnotify 监听 persona/ 目录 → 模板文件变了自动重载
方案B：管理命令 /persona reload → 手动触发热重载
方案C：两者都支持 —— 文件监控 + 手动命令兜底
```

这个思路完全不同——**人格由 Agent 自主演化，不是静态配置文件**。

## 重新设计

### 核心思路

```
Agent 在对话中感知：
  "这个群全是技术讨论，我太随意了"
       │
       ▼
  Agent 调 tool: update_persona
       │
       ├─→ 内存中立即生效（当前及后续对话用新人格）
       └─→ 异步写入 SQLite（重启不丢）
```

人格不是管理者配置的，是 **Agent 自己适应环境后修剪出来的**。

### SQLite 表设计（极简）

```
persona
  id          TEXT    PK    — "default"、"tech:67890"（群级人格）
  extends     TEXT          — 继承链，如 "default"
  prompt      TEXT          — 系统 prompt 正文
  tone        TEXT          — 语气基调
  traits      TEXT          — 性格标签 JSON
  examples    TEXT          — few-shot 示例 JSON
  version     INTEGER       — 递增版本号
  updated_at  TEXT
```

继承链就是 extends 字段连成一条链，合并时从子到父逐层覆盖。

### Agent 更新人格的 Tool

Agent 不直接改 SQL，而是通过 tool：

```
Agent 调: update_persona(target="group:67890", changes={
    tone: "更专业一点",
    traits: ["技术宅", "偶尔毒舌"]
})

→ 合并 extends 链生成完整 prompt
→ 更新内存，立即生效
→ 异步写 SQLite
```

---

有个点需要确认：

好，完全自包含——SQLite 初始化即交付，零文件依赖。更新文档。

先加 `persona` 表到 9.1，然后在待讨论里划掉人格系统，再在相应位置插入人格系统章节。

不小心覆盖了 bot_state，补回来。

现在在第 6 节后插入人格系统章节。

重新编号后续章节，并划掉待讨论中的人格系统。

人格系统已落盘。剩余待讨论：

- 事件处理管线
- Agent 记忆更新闭环
- 情绪/状态系统
- 安全与风控

继续哪个？

## User

表设计为id，userid，groupid（为父时id为0），extend（一父多子），tone，traits

## Assistant

同步更新 7.3 和插件系统的子节编号。

一个需要确认的点：当前 `persona` 表有 `userid` 字段，但全局人格（groupid=0）和群级人格（groupid≠0）都还没用到它。这个字段是给私聊人格预留的？

还是先继续往下聊下一个话题——事件处理、记忆闭环、情绪系统、安全风控，你选哪个？

## User

人格绑定无需专属文件，当查询带userid与groupid一致时，同过extend（存放父id）查询到父persona

## Assistant

简洁——表内自闭合，extend 链即绑定。

继续下一个话题，你来选：事件管线、记忆闭环、情绪系统、安全风控？

## User

向量检索是否有无需依赖外部服务的

## Assistant

## 纯 Go vs CGO 方案对比

| 方案 | CGO | 外部服务 | 实现方式 |
|------|:---:|:---:|------|
| **内存暴力搜索** | ❌ 无 | ❌ 无 | embedding 调 API → 存 SQLite BLOB → 查询时加载内存 → 自己写余弦相似度 |
| **sqlite-vec** | ✅ 需要 | ❌ 无 | SQLite 扩展，嵌入进程，`go get` + C 编译即可 |
| **kelindar/search** | ✅ 需要 | ❌ 无 | 555⭐，但内置 llama.cpp 本地跑 embedding，太重 |

---

## 我的判断：选内存暴力搜索

**bot 的场景数据量小**（几百到几千条记忆），暴力搜索完全够用：

```
embedding → 调 OpenAI / DeepSeek API 生成向量
          → 存 SQLite BLOB 字段
          
查询时     → SELECT 该群相关消息
          → 加载到内存（几百个 1536 维向量）
          → 逐个算余弦相似度，TopK
          → 几毫秒完事
```

零 CGO、零扩展、纯 Go，几行代码。而且 eino 本身调用 embedding API 的链路是现成的。

等到数据量真大到需要索引（几百个群、几万条记忆），再接入 sqlite-vec 也只是改 SQL 语句。

---

这个方案可以写到文档里，替换之前模糊的"后续可选 sqlite-vec"。

## User

每次对话前构建消息时仅保留动态对话窗口中人物的画像，但是后续压缩对话出现A对话消失，此时将A的画像通过字段n轮后才移除，注意sql读取仅在必要时读取，不要每次构建上下文时都调用sql，读取后写入memory层的结构体即可

## Assistant

已更新。这个设计的精髓在于：**内存里的画像数量 = 当前活跃的用户数**，而不是全群人数。延迟淘汰保证了一个人短暂离开窗口再回来时不用重新查 SQL。

## User

融合后的摘要不要归档

## Assistant

已移除归档概念。摘要现在纯内存流转，不落盘。

现在整条记忆链路是纯内存流转，不落地也不归档：

```
窗口满 → 压缩为摘要 → 摘要攒多了 → 二级融合 → 满了就丢
```

还剩这几个待讨论，继续哪个？

2. **事件处理管线**
3. **Agent 记忆更新闭环**
4. **情绪/状态系统**
5. **安全与风控**

## User

移除用户画像的语气（没用）

## Assistant

文档中两处可能涉及"语气"：

1. **persona 表**（7.2 节）的 `tone` 字段 — bot 各群人格的语气
2. **member_profile**（4.2 节）中的"发言风格标签"

你指的是移除 persona 的 `tone`，还是 member_profile 中不要含语气相关的内容，还是两个都去掉？

## User

都要，了解用户语气没有实际作用

## Assistant

已移除：两边的 tone 都去掉了，member_profile 也不再记录发言风格标签。

继续下一个话题：**事件处理管线、记忆更新闭环、情绪/状态系统、安全风控**，你选哪个？

## User

插件设计是否可以通过接口，然后自定义的go包只需实现接口就可以加载，还是当前设计

## Assistant

两个方案可以互补，不必二选一。现状是：

| | 内建插件 | exe 插件 |
|------|:---:|:---:|
| 实现方式 | Go 包实现接口，`init()` 注册 | 独立进程，stdio JSON |
| 语言限制 | Go | 任意 |
| 需要重编译 | ✅ | ❌ |
| 进程隔离 | ❌（同进程） | ✅ |
| 部署 | 一起打包 | 丢进 `plugins/` 目录即可 |

内建插件走 Go 接口，exe 插件走 stdio 协议——**底层接口不同但上层统一**，对外都是 `Execute(ctx) → Reply`。

你是想确认这个，还是倾向于砍掉其中一个方案？

## User

第一个方案不是可以通过加载内存吗

## Assistant

你说的是 Go 的 `plugin.Open()` 动态加载 `.so`，只在 Linux 上可行，Windows 不支持。

所以实际上就两条路，不多不少正好互补：

| | 编译时接口 | exe 子进程 |
|------|:---:|:---:|
| 原理 | Go 包实现接口，`init()` 注册，编译进二进制 | exec 启动独立进程 |
| 跨平台 | ✅ | ✅ |
| 不用重编译 | ❌ | ✅ |
| 跨语言 | ❌ | ✅ |

Windows 下没有动态库加载这条路，所以两套方案已经是最简覆盖。要么跟着编译，要么走独立 exe。当前文档没问题。

## User

选择plugin.Open() 动态加载 .so，exe可以作为可添加方案

## Assistant

修复重复编号。

已更新。主方案是 `.so` 动态加载 + Go 接口，exe 作为可添加方案保留。

继续哪个？事件管线、记忆更新闭环、情绪系统、安全风控？

## User

事件管线

## Assistant

OneBot 事件管线的核心问题是：**哪些事件走 Agent，哪些走规则，哪些忽略**。

## OneBot v11 事件全景

| 事件类型 | 子类型 | 处理策略 |
|------|------|------|
| **message** | group / private | 核心管线（路由 → 命令/Agent） |
| **notice** | group_increase | 更新群画像 + 可选欢迎语（Agent 可介入） |
|  | group_decrease | 更新群画像 |
|  | poke | 可选回应 |
|  | group_ban / lift_ban | 记录日志 |
|  | group_admin | 更新权限信息 |
|  | notify (戳一戳/红包/等等) | 按需 |
| **request** | friend / group | 规则引擎（自动通过/拒绝策略） |
| **meta_event** | heartbeat / lifecycle | 状态更新（在线时间/连接状态） |

## 消息管线（核心）

```
消息进入
  │
  ├── [中间件] 日志         ← 所有消息都打日志
  ├── [中间件] 敏感词过滤    ← 包含敏感词则拦截
  ├── [中间件] 限流          ← 短时间大量消息则降级
  │
  ├── 是命令 (/开头)？ → 插件分发 → 直接返回
  │
  └── 是普通消息？
        │
        ├── 私聊 → 必然走 Agent
        │
        └── 群聊 → mention 模式？
              ├── @bot → 走 Agent
              └── 未@ → auto 模式？ → Agent 判断 → 回复/忽略
```

## 通知管线（轻量）

```
通知进入
  │
  ├── 群成员增加 → 群人+1，可配欢迎语（走 Agent 生成 → 发群里）
  ├── 群成员减少 → 群人-1，记录
  ├── 戳一戳     → 简单回应（"？"）
  ├── 禁言/解禁  → 记录
  └── 其他       → 忽略（插件可订阅扩展）
```

---

更新文档。把事件管线作为第 10 节插入，后续编号后移。

重编号后续章节。

已落盘。剩余待讨论只剩三个了：

- [ ] Agent 记忆更新闭环
- [ ] 情绪/状态系统
- [ ] 安全与风控

继续哪个？

## User

Agent 记忆更新闭环

## Assistant

核心问题：Agent 在对话中顺带更新记忆，不需要额外的分析管道。关键设计点四个：

## 1. 输出结构：回复 + 记忆指令

```
Agent 一次推理，两份输出：

{
  "reply": "Rust 不错啊，学到所有权了没？",     ← 发到群里
  "memory_ops": [                               ← 内部消费
    { "action": "store_fact", "user": 12345, "content": "正在学 Rust" },
    { "action": "update_tag",  "user": 12345, "tag": "程序员" }
  ]
}
```

记忆指令不占上下文，不对外可见，仅内部执行。

## 2. 更新策略分层

| 记忆类型 | 更新频率 | 触发者 |
|------|------|------|
| member_facts（事实） | 实时，对话中自然提取 | Agent |
| member_profile 活跃度 | 实时，纯统计规则 | 规则层 |
| member_profile 兴趣话题 | 实时，对话中提取 | Agent |
| group_jargon（黑话） | 积累 + 人工标记 | Agent 检测 → 待确认 |
| group_profile 氛围标签 | 定频（每天/每周） | 定时 LLM 分析 |

## 3. 冲突处理

```
用户先说 "我是前端" → store_fact
用户后说 "我转后端了" → 

Agent 检测到冲突：
  → update_fact 覆盖旧内容
  → 或标记旧事实为 "过期"
  → 新旧都保留（带时间戳），Agent 判断时优先取最新的
```

放在第 4 节作为 4.4，保持记忆系统完整。

已落盘。剩余两个：

- [ ] 情绪/状态系统
- [ ] 安全与风控

选哪个？

## User

你说得记忆类型是什么，为什么ai回复有memory_ops，你是不是不了解eino框架

## Assistant

你说得对，我把 eino 的 tool calling 模式理解成了结构化 JSON 输出。Agent 就是纯文本回复，记忆更新应该通过 eino Tool 实现。

## 纠正后的设计

```
Agent 收到 "我最近在学 Rust"

Agent 推理 → 
  1. 回复: "Rust 不错啊，学到所有权了没？"     ← 自然语言，发群里
  2. 调 tool: store_fact(user=12345, "正在学 Rust")  ← eino Tool
  3. 完成
```

记忆更新就是 eino Tool，和查天气、搜网页一样，Agent 在 ReAct 循环中自行决定调不调。

更新文档。

已纠正。Agent 回复就是自然语言，记忆更新走 eino Tool 调用，不是结构化 JSON 输出。

剩余两个：**情绪/状态系统**、**安全与风控**，选哪个？

## User

记忆类型是什么

## Assistant

那张表最右列的触发者其实只有两类：**Agent Tool** 和 **规则层**。表写复杂了。简化成两句话就够了：

- Agent Tool 负责更新：`member_facts`、`member_profile` 兴趣标签、`group_jargon`
- 规则层负责更新：活跃度统计、群氛围标签（定时任务）

要我把文档里那张表替换掉吗？还是你有更具体的问题？

## User

移除兴趣话题，当前表设计没有这个，不对，你当前整个记忆类型都有问题，eino框架发送消息是text，没有结构化，请不要先设计具体结构体

## Assistant

你说得对，我在架构文档里过早设计具体结构体了。精简。

已精简。我记住了：架构文档只描述做什么，不设计具体结构体。Tool 名称、字段细节都是实现时才定。

继续情绪/状态系统还是安全风控？

## User

情绪/状态系统

## Assistant

架构层面先理清三个问题：

## 状态维度

触发模式那节已经隐含了状态驱动的判断——精力、连续回复次数。除此之外是否还需要：

| 维度 | 作用 |
|------|------|
| 精力 | 影响是否回复（已用） |
| 连续回复计数 | 防止刷屏（已用） |
| 心情 | 是否需要？影响回复风格？ |

## 状态是规则层还是 Agent 层？

两条路：
- **纯规则**：状态只用于预过滤器（精力 < 3 不回复），不改 Agent 行为。Agent 不感知状态
- **Agent 感知**：状态数值/描述写入 prompt，Agent 根据状态调整回复风格

## 状态是否需要持久化？

- 如果纯规则：内存即可，重启归零问题不大
- 如果要"今天心情一直不好"这种跨重启的：需要 SQLite

你的想法？

## User

规则层，讨论规则

## Assistant

规则层几个维度：

| 规则 | 作用 | 是否需要 |
|------|------|:---:|
| 精力值 | 每次回复消耗，随时间恢复，低于阈值不主动说话 | |
| 连续回复上限 | 连续说了 N 句后强制休息 | ✅ 已定 |
| 冷却时间 | 两次主动回复最小间隔，防刷屏 | |
| 时段控制 | 深夜/凌晨不参与对话（除非 @） | ✅ 已定 |
| 每日主动上限 | 过了上限只响应 @ | |
| 消息速率感知 | 群里聊得快时可以更频繁参与，聊得慢就少说 | |

哪些需要，哪些多余？

## User

每日主动上限多余， 消息速率感知多余

## Assistant

你保留的四个规则和触发模式那节里的预过滤器天然是一体的。我直接把状态规则合并到触发模式那节里。

已整合。只剩最后一个：**安全与风控**，继续？

## User

继续

## Assistant

安全与风控可以从几个角度切：

| 层面 | 内容 | 架构文档要不要写 |
|------|------|:---:|
| **敏感词过滤** | 消息管线中间件已纳入 | ✅ 已定 |
| **限流** | 消息管线中间件已纳入 | ✅ 已定 |
| **权限分级** | 超级管理员/群管/普通用户，控制命令和 Tool 的可用范围 | |
| **LLM 成本控制** | 每天/每小时调用上限，防止刷爆 API 费用 | |
| **防封号** | 行为拟人化、消息间隔随机化等 | |

防封号属于运维策略，不在架构里写。你觉得权限分级和 LLM 成本控制需要写入吗？

## User

都不用了

就  限流， 敏感词过滤

## Assistant

已在消息管线中间件里了。直接划掉待讨论，架构文档全部完成。

架构讨论全部完成。13 章、零待讨论项，`docs/architecture.md` 覆盖了从连接层到记忆闭环的所有决策。

要开始搭项目骨架了吗？

